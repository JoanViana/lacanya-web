{% extends 'DWSBundle::layout.html.twig' %}
{% trans_default_domain 'DWSBundle' %} 
{% block title %}DWS - {{ 'product.edit.title'|trans({'%productname%' : product.name }) }}{% endblock %}

{% block body %}
    {{ parent() }}
	<div class="main container">
		<div class="row well">
			<div class="col-md-6 col-md-offset-3">
				<div class="page-header">
					<h2>{{ 'product.edit.header'|trans({'%productname%' : product.name }) }}</h2>
				</div>
                {{ form_start(form, { 'attr' : {'novalidate': 'novalidate', 'role' : 'form'} }) }}
                <fieldset>
                <div class="form-group">
                    {{ form_label(form.name) }}
                    {{ form_widget(form.name, { 'attr' : {'class' : 'form-control' } }) }}
                    <span class="text-danger">{{ form_errors(form.name) }}</span>
                </div>
                
                <div class="form-group">
			        {{ form_label(form.category) }}
			        {{ form_widget(form.category, {'attr': {'class': 'form-control'}}) }}
			        <span class="text-danger">{{ form_errors(form.category) }}</span>
			    </div>

			    <div class="form-group">
			        {{ form_label(form.price) }}
			        {{ form_widget(form.price, {'attr': {'class': 'form-control' }}) }}
			        <span class="text-danger">{{ form_errors(form.price) }}</span>
			    </div>
			    
			    <div class="form-group">
			        {{ form_label(form.description) }}
			        {{ form_widget(form.description, {'attr': {'class': 'form-control' }}) }}
			        <span class="text-danger">{{ form_errors(form.price) }}</span>
			    </div>

			</fieldset>

				<div class="row">
					<div class="col-sm-12">
					    <p>
					        {{ form_widget(form.save, {'attr': {'class': 'btn btn-success'}}) }}

					    </p>        
			                {{ form_end(form) }}
                	</div>
				</div>
			</div>
		</div>

	</div>
{% endblock %}

