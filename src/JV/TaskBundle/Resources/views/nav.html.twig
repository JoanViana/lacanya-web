{% trans_default_domain 'DWSBundle' %} 

<nav class="navbar navbar-inverse">
    <div class="container-fluid container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand">@DWSBundle</a>
            <!-- <p class="navbar-text hidden-xs"><small>{{ 'exp.by' | trans }} Joan Dídac Viana Fons</small></p>-->
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
		        <li><a href="{{ path('index') }}">
		             <span class="glyphicon glyphicon-home" aria-hidden="true"></span>  
		             {{ 'general.home' | trans }} <span class="sr-only">(current)</span></a></li>
	        
		        <li class="dropdown">
		          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">{{ 'general.person' | trans }} <span class="caret"></span></a>
		          <ul class="dropdown-menu">
		            <li><a href="{{ path('per_list') }}"> 
		             <span class="glyphicon glyphicon-list" aria-hidden="true"></span> 
		             {{ 'person.list.title' | trans }}</a></li>
		            {% if is_granted('ROLE_APP_ADMIN') %}
                    <li role="separator" class="divider"></li>
		            <li><a href="{{ path('per_new') }}"> 
		            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>  
		            {{ 'person.new.title' | trans }}</a></li>
		            <li><a href="{{ path('per_create_def') }}"> 
		            <span class="glyphicon glyphicon-record" aria-hidden="true"></span>  
		            {{ 'person.create.title' | trans }}</a></li>
                    {% endif %}
		          </ul>
		        </li>
		        
		        <li class="dropdown">
		          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">{{ 'general.category' | trans }} <span class="caret"></span></a>
		          <ul class="dropdown-menu">
		            <li><a href="{{ path('cat_list') }}">  <span class="glyphicon glyphicon-list" aria-hidden="true"></span> 
		            {{ 'category.list.title' | trans }}</a></li>
		            {% if is_granted('ROLE_APP_ADMIN') %}
		            <li role="separator" class="divider"></li>
		            <li><a href="{{ path('cat_new') }}"> 
		            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> 
		            {{ 'category.new.title' | trans }}</a></li>
		            <li><a href="{{ path('cat_create_def') }}"> 
		            <span class="glyphicon glyphicon-record" aria-hidden="true"></span>  
		            {{ 'category.create.title' | trans }}</a></li>
		            {% endif %}
		          </ul>
		        </li>
		        
		        <li class="dropdown">
		          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">{{ 'general.product' | trans }} <span class="caret"></span></a>
		          <ul class="dropdown-menu">
		            <li><a href="{{ path('pro_list') }}"> <span class="glyphicon glyphicon-list" aria-hidden="true"></span>  
		            {{ 'product.list.title' | trans }}</a></li>
		            <li><a href="{{ path('pro_list_allbycat') }}"> 
		              <span class="glyphicon glyphicon-th" aria-hidden="true"></span>
		              {{ 'product.listAllByCat.title' | trans }}</a></li>
		            {% if is_granted('ROLE_APP_ADMIN') %}
		            <li role="separator" class="divider"></li>
		            <li><a href="{{ path('pro_new') }}"> 
		            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> 
		            {{ 'product.new.title' | trans }}</a></li>
		            <li><a href="{{ path('pro_create_def') }}"> 
		            <span class="glyphicon glyphicon-record" aria-hidden="true"></span>  
		            {{ 'product.create.title' | trans }}</a></li>
		            {% endif %}
		          </ul>
		        </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">                    
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown"
                    role="button" aria-haspopup="true" aria-expanded="false">
                    <span class="glyphicon glyphicon-flag"></span> {{ app.request.locale }} <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ path(app.request.get('_route'), app.request.get('_route_params')|merge({'_locale': 'es'})) }}">
                            es</a></li>                          
                        <li><a href="{{ path(app.request.get('_route'), app.request.get('_route_params')|merge({'_locale': 'en'})) }}">
                            en</a></li>  
                    </ul>
                </li> 
            </ul>
                {#
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <p class="navbar-text navbar-right hidden-xs">
                        {{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }} | 
                        <a href="{{ path('fos_user_security_logout') }}">
                            {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                        </a>
                    </p>
                #}
                    {#<button type="button" class="btn btn-default navbar-btn navbar-right">Logout</button>#}
                {% if is_granted("IS_AUTHENTICATED_FULLY") %}
                      <ul class="nav navbar-nav navbar-right">
                        <li class="dropdown">
                          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                               <span class="glyphicon glyphicon-user" aria-hidden="true"></span> 
                               {{ app.user.username}} <span class="caret"></span></a>
                          <ul class="dropdown-menu">
                            <li><a href="{{ path('fos_user_profile_show') }}">
                                <span class="glyphicon glyphicon-info-sign" aria-hidden="true"></span> 
                                {{ 'user.show.title'|trans }}</a></li>
                            <li><a href="{{ path('fos_user_profile_edit') }}">
                                <span class="glyphicon glyphicon-edit" aria-hidden="true"></span>
                                {{ 'user.edit.title'|trans }}</a></li>
                            <li><a href="{{ path('fos_user_change_password') }}"> 
                            <span class="glyphicon glyphicon-wrench" aria-hidden="true"></span> 
                            {{ 'user.pass.title'|trans }}</a></li>
                            <li role="separator" class="divider"></li>
                            <li><a href="{{ path('fos_user_security_logout') }}"> 
                            <span class="glyphicon glyphicon-log-out" aria-hidden="true"></span> 
                            {{ 'action.logout'|trans }}</a></li>
                          </ul>
                        </li>
                      </ul>
                {% else %}
                <form class="navbar-form navbar-right form-inline">
                    <div class="form-group form-group-sm">
                        <a href="{{ path('fos_user_registration_register') }}" class="btn btn-sm btn-success navbar-right"><strong>{{ 'action.register' | trans }}</strong></a>
                    </div>
                </form>
                
            	<form class="navbar-form navbar-right form-inline" action="{{ path("fos_user_security_check") }}" method="post" name='authenticate'>
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}" />
                    <fieldset>
                        <div class="form-group form-group-sm">
                            <input type="text" class="form-control" id="username" name="_username" placeholder={{ 'form.emailUsername' | trans }} required="required">
                        </div>
                        <div class="form-group form-group-sm">
                            <input type="password" class="form-control" id="password" name="_password" placeholder={{ 'form.password' | trans }} required="required">
                        </div> 
                        <div class="form-group form-group-sm">
                            <a><input href="{{ path('fos_user_security_login') }}" class="btn btn-sm btn-info" type="submit" id="_submit" name="_submit" value={{ 'action.login' | trans }} /></a>                        
                        </div>
                    </fieldset>
                </form>

                {% endif %}
                

        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>